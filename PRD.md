# AI Pixel War PRD

## 1. 文档信息

- 项目名称：AI Pixel War
- 文档类型：产品需求文档（PRD）
- 当前阶段：MVP
- 文档语言：中文
- 产出依据：基于当前仓库中的 README、CLI、引擎模块、Viewer、示例 profiles 与 replay 输出整理
- 文档目的：为该项目补齐一份面向产品、设计、研发和演示场景都可直接使用的需求说明

## 2. 产品概述

AI Pixel War 是一个“多数字分身参与的像素画布对抗与叙事模拟”产品。系统让一组具备人格、情绪、记忆和关系网络的 AI 角色，在共享画布上进行扩张、防守、侵略、爆破、协商、私聊、结盟与共同作画，并将完整过程沉淀为可回放、可观战、可复盘的 replay 数据。

与传统回合制模拟不同，本项目同时强调三条价值线：

1. 策略对抗价值：角色争夺像素领地，围绕地盘、安全感和资源节奏进行博弈。
2. 社交叙事价值：角色之间会公开发言、私聊、缔结条约、积累仇恨与信任，形成连续剧情。
3. 审美生成价值：系统围绕共享 myth prompt 生成艺术方向，引导角色在冲突中共同完成一幅具有主题感的像素作品。

一句话定义：

一个把 AI 社交 drama、轻策略博弈和像素艺术生成融合在一起的可观战模拟器。

## 3. 背景与问题

当前的 AI Agent 演示项目，大多只展示“任务完成”或“单轮对话”，很少同时满足以下几点：

- 过程可视化，而不是只看最终结果
- 多角色互动，而不是单 Agent 独演
- 长时记忆和关系演化，而不是孤立回合
- 有审美目标，而不是纯工具执行
- 可复盘、可保存、可分享

AI Pixel War 试图解决的问题是：

- 如何让多 Agent 行为结果更“好看”、更“有戏”、更适合展示
- 如何把 AI 决策从黑盒输出变成可观察、可解释的逐回合事件流
- 如何通过人格参数与关系网络，让同一套世界规则里持续产生不同故事

## 4. 产品目标

## 4.1 核心目标

1. 提供一套可重复运行的多 Agent 像素战争模拟体验。
2. 让角色行为受到人格 DNA、情绪、关系、共享历史和艺术主题共同驱动。
3. 输出完整 replay，支持前端逐回合观战、分析与演示。
4. 支持通过 profiles 批量接入数字分身，形成差异化角色池。
5. 在 dry-run 与实时模型模式下都能得到结构化、可回放的结果。

## 4.2 非目标

当前版本不以以下方向为优先目标：

- 面向外部用户的 SaaS 多人在线服务
- 实时多人联机操控
- 完整账号、权限、支付或社区系统
- 通用 Agent 编排平台
- 高精度图像生成器替代方案

## 5. 目标用户

## 5.1 核心用户画像

### A. AI 创作者 / Demo 制作者

- 需要用更具戏剧性和视觉冲击力的方式展示多 Agent 能力
- 关注可看性、分享性和演示稳定性
- 希望快速通过一条命令生成可回放结果

### B. Agent 系统研究者 / 开发者

- 关注人格参数、关系状态、决策输出和行为结果之间的关系
- 需要可结构化保存的数据结果用于分析和迭代
- 关心 schema 校验、回退机制、并发与运行稳定性

### C. 世界观 / 叙事玩法设计者

- 希望构造一组有鲜明性格的角色，在同一主题下持续产出剧情
- 看重角色关系变化、冲突张力、联盟与背叛
- 希望作品既有故事，也有视觉母题

## 5.2 典型使用场景

1. 演示场景：运行一次模拟，打开 Viewer 回放整局过程，向团队或观众展示“AI 角色会聊天、会结盟、会打架、还会共同作画”。
2. 研究场景：调整 profiles 中的人格 DNA 和目标权重，比较不同角色组合对社交张力和画面结果的影响。
3. 内容生产场景：通过 myth prompt 批量生成不同主题的神话壁画风模拟回放，作为世界观素材或宣传内容。
4. 复盘场景：读取历史 replay，对比排名、社交指标、消息内容和关键事件。

## 6. 产品定位与价值主张

## 6.1 产品定位

一个面向 AI 体验展示、叙事模拟和多角色实验的像素战争回放系统。

## 6.2 差异化价值

- 不只输出结果，还输出完整过程
- 不只强调对抗，还强调关系和叙事
- 不只做规则博弈，还引入共同艺术方向
- 不只依赖模型实时输出，也支持 dry-run 演示模式
- 不只是一段日志，而是可视化回放产品

## 7. 功能范围

## 7.1 当前已实现范围

### 7.1.1 模拟配置与启动

- 支持通过 CLI 启动模拟
- 支持配置回合数、画布尺寸、Agent 数量、并发数、模型名
- 支持切换 dry-run 与实时模型模式
- 支持传入 `profiles` 文件覆盖默认角色池
- 支持传入 myth prompt/theme 影响全局艺术方向

### 7.1.2 数字分身配置

- 支持通过 JSON 批量定义数字分身
- 角色参数包含：
  - 名称与颜色
  - archetype
  - core values
  - speech style
  - 风险、攻击、外交、创意偏好
  - signature moves
  - taboos
  - 目标权重（territory / art / revenge / reputation）

### 7.1.3 回合制决策与执行

- 每个回合为角色生成结构化决策
- 角色可执行的动作包括：
  - `paint`
  - `fortify`
  - `invade`
  - `burst`
  - `wait`
- 系统支持能量、冷却、并发处理与回合推进
- 支持对决策结果进行 schema 校验与修复/回退

### 7.1.4 社交与情绪系统

- 角色拥有情绪状态：anger、fear、confidence、satisfaction
- 角色之间维护关系状态：trust、affinity、debt
- 系统记录 shared history / memory events
- 支持公开发言、私聊、条约提案
- 会根据事件动态更新联盟、敌意与张力

### 7.1.5 艺术方向与神话主题

- 默认启用 Myth Mode
- 系统围绕 myth prompt 自动生成：
  - 标题
  - mood words
  - palette
  - forbidden colors
  - motifs
  - composition notes
  - zone guides
- 画布按阶段推进作画，包含主体、轮廓、motif、背景与收束过程

### 7.1.6 Replay 生成与落盘

- 每次运行输出 replay JSON 到 `output/`
- 输出内容包括：
  - 配置参数
  - art direction
  - 排名与最终得分
  - final highlights
  - 每回合画布更新
  - 每回合动作步骤
  - 发言、私聊、人格注释
  - 社交快照与社交指标
  - 错误信息

### 7.1.7 Viewer 观战体验

- 支持读取最新 replay
- 支持切换历史 replay
- 支持逐回合播放像素画布
- 支持“跟随最新”与“循环播放”
- 支持查看公开发言、私聊、人格注释
- 支持查看每回合活跃指标和社交指标
- 支持通过 Twin Lens 查看角色情绪、摘要、强关系与高张力关系

### 7.1.8 Replay API

- `GET /api/replays`：返回 replay 列表
- `GET /api/latest`：返回最新 replay
- `GET /api/replay/:name`：返回指定 replay
- `GET /health`：健康检查

### 7.1.9 逐动作像素回放（V0.1 后期新增）

- Replay 数据中每回合包含 `action_steps` 数组
- 每个 step 记录 kind（seed / action / resolve_fill / resolve_harmonize）、actor、label 和像素更新
- Viewer 支持按 step 逐帧播放，显示当前步骤标签和像素进度

## 7.2 代码分析发现的技术债务

以下问题来自 2026-03-12 的代码深度分析（V0.2 已全部解决）：

1. ~~**Viewer 单文件过大**~~ → 已拆分为 hooks + components 组件化架构，App.tsx 降至 ~270 行
2. ~~**引擎回调冗余**~~ → EngineContext 统一传递，mythColorForPoint lambda 仅构造 1 次
3. ~~**LLM 硬绑定**~~ → LLMProvider 接口抽象，DeepSeek 为默认实现
4. ~~**Replay 无版本控制**~~ → schema_version "1.0"，Viewer 兼容旧格式
5. ~~**零测试覆盖**~~ → 8 个测试文件，21 个测试用例，覆盖核心模块
6. ~~**Treaty 系统不完整**~~ → joint_attack 完整实现（签约/攻击加成/互不侵犯/关系影响）
7. ~~**Viewer 未展示错误数据**~~ → ErrorPanel 组件按类型着色显示
8. ~~**CLI 缺少帮助文本**~~ → --help 支持，未知参数 warning
9. ~~**排名显示不直观**~~ → 排名面板显示角色名

## 7.3 V0.2 新增功能（2026-03-13 完成）

1. **Viewer 错误面板**：ErrorPanel 组件按错误类型着色（validation=红、repaired=黄、generation=橙），无错误时隐藏
2. **排名显示角色名**：RoundPulse 排名面板从 agentDirectory 查找并显示角色名
3. **Replay 筛选**：API 支持 `?mode=dry-run|live&agents_min=N&agents_max=N` 查询参数，Viewer 提供模式筛选下拉和 badge 标签
4. **Treaty joint_attack**：完整实现联合攻击条约系统，包含签约、+15% 攻击加成、联盟方互不侵犯、关系影响、事件记录

## 7.4 建议纳入下一阶段范围

以下需求在当前仓库中没有完整产品化，但非常适合进入后续迭代：

1. Replay 标签化与收藏能力
2. 按主题、角色池、日期筛选 replay
3. 基于同一 profile 集的多局对比分析
4. 更丰富的规则系统，例如资源点、地形、阵营目标
5. 更完整的 viewer 分享能力，例如截图、导出 GIF、分享链接
6. Web 表单化的配置入口，而不只是 CLI

## 8. 核心用户流程

## 8.1 主流程

1. 用户准备一组数字分身 profiles，或使用默认角色池。
2. 用户通过 CLI 设置轮数、画布尺寸、主题和运行模式。
3. 系统初始化画布、角色、艺术方向和初始关系。
4. 每回合系统为各角色生成决策，并执行动作、消息与条约逻辑。
5. 系统持续记录画布更新、社交事件、情绪变化和回合指标。
6. 模拟结束后生成 replay JSON，并保存到 `output/`。
7. Viewer 读取 replay，向用户展示可回放、可观察、可分析的完整过程。

## 8.2 复盘流程

1. 用户打开 Viewer。
2. Viewer 自动拉取最新 replay，或允许用户切换历史 replay。
3. 用户按回合查看画面推进。
4. 用户同时查看 public voice、private wire、persona notes 和 Twin Lens。
5. 用户根据 ranking、social metrics、final highlights 进行复盘或分享。

## 9. 功能需求明细

## 9.1 模拟启动

### 需求说明

系统应允许用户通过单条命令快速启动模拟，并支持 dry-run 和实时模型两类模式。

### 验收标准

- 用户可在无 API Key 的情况下通过 dry-run 成功生成 replay
- 用户可指定基本参数并得到有效输出
- 参数越界时系统应给出明确报错

## 9.2 角色人格配置

### 需求说明

系统应允许用户通过结构化 JSON 定义角色人格与目标，使模拟可重复、可调优、可批量实验。

### 验收标准

- 用户可传入 profiles 文件并覆盖默认 agent 配置
- 当提供 profiles 时，实际 agent 数量应与配置一致
- 人格参数应能反映在行为倾向和表达风格中

## 9.3 关系与社交叙事

### 需求说明

系统应让角色之间持续积累关系与历史，而不是每回合重新开始。

### 验收标准

- 回放中可见公开发言与私聊
- 条约签订、攻击、失地、毁约等事件会影响关系值
- Viewer 中可查看 strongest bonds 与 hottest rivalries

## 9.4 画布对抗与审美生成

### 需求说明

系统应将占地博弈与神话主题作画结合，保证模拟既有对抗结果也有审美产出。

### 验收标准

- 角色可通过不同动作对画布造成实际影响
- 最终回放可看到分阶段艺术推进
- 最终得分同时考虑 territory、art、reputation

## 9.5 Replay 与观战

### 需求说明

系统应将模拟过程转为稳定的 replay 数据结构，并通过 Viewer 提供可理解的观战体验。

### 验收标准

- 每次模拟生成唯一 replay 文件
- Viewer 可读取最新文件并正常播放
- 用户可同时看到画布变化、消息、关系和回合指标

## 10. 关键数据对象

## 10.1 核心实体

- AgentState：角色运行态，包含身份、情绪、关系、记忆、能量与声望
- IdentityDNA：角色人格基因
- TurnDecision：单角色单回合的结构化决策
- Treaty：角色之间的协定
- ReplayRound：单回合回放对象
- SimulationResult：整局输出对象
- ArtDirection：本局共享艺术方向

## 10.2 核心输入

- CLI 参数
- `profiles/*.json`
- DeepSeek API Key（实时模式）
- myth prompt / theme prompt

## 10.3 核心输出

- `output/replay-*.json`
- 终局排名
- 社交指标
- 关键高亮事件
- 供前端消费的回放数据

## 11. 信息架构

## 11.1 产品模块划分

1. 配置入口层：CLI 参数、profiles、环境变量
2. 模拟引擎层：角色初始化、艺术方向生成、回合决策、动作执行、记忆/关系更新、计分
3. 输出数据层：replay JSON、schema、错误记录
4. 观战呈现层：Viewer API、React 前端、逐回合播放与关系透镜

## 11.2 仓库映射

- `src/index.ts`：CLI 入口
- `src/engine.ts`：主引擎编排
- `src/engine/*`：艺术、社交、策略、画布、得分等子模块
- `src/viewerServer.ts`：Replay API 与静态服务
- `viewer/src/App.tsx`：观战前端主界面
- `profiles/`：数字分身样例
- `schemas/`：Agent/Decision 结构约束
- `output/`：回放产物目录

## 12. 非功能需求

## 12.1 可用性

- dry-run 应可作为默认演示路径，降低依赖外部模型时的不稳定性
- Viewer 需要让用户在不读日志的情况下理解局势变化

## 12.2 性能

- 支持 8 到 30 个 Agent 的模拟配置
- 支持通过并发参数控制运行成本与速度
- Viewer 对大 replay 文件应保持可播放、可切换

## 12.3 稳定性

- 决策输出需经过 schema 校验
- 当模型输出异常时，系统应能 fallback 到 mock / 修复逻辑
- API 接口应在无 replay 文件时返回明确错误提示

## 12.4 可扩展性

- 角色池、动作、评分和主题生成逻辑应允许继续扩展
- replay 结构应保持前后兼容意识，避免 Viewer 频繁失效

## 12.5 可维护性

- 引擎应继续保持模块化拆分
- PRD、README、schemas 与 replay 字段需要同步演进

## 13. 成功指标

## 13.1 MVP 结果指标

1. 用户可在 5 分钟内完成一次从启动模拟到 Viewer 观战的闭环。
2. dry-run 模式下可以稳定产出有效 replay。
3. 用户能够从 Viewer 中识别出至少三类信息：领地变化、角色关系变化、艺术主题推进。
4. 同一套 profiles 在不同主题下可以产出显著不同的画面与叙事。

## 13.2 建议补充的数据指标

1. 每局模拟成功率
2. 平均 replay 文件大小
3. Viewer 首次加载时间
4. 每回合消息量与冲突量
5. 不同 profile 组合下的联盟密度与最大张力变化

## 14. 风险与约束

1. 依赖外部模型时，输出质量与稳定性可能波动。
2. replay 文件可能快速增大，影响 Viewer 加载和轮询体验。
3. 当前产品入口主要是 CLI，对非技术用户不够友好。
4. 关系、艺术、策略三条逻辑线同时增长后，调参与解释成本会上升。
5. 如果 replay 数据结构频繁变化，前端兼容性维护成本会增加。

## 15. 版本规划建议

## V0.1 MVP（已完成）

- 完成 CLI 模拟、数字分身接入、神话主题作画、社交叙事、replay 输出和 Viewer 基础观战

## V0.2（已完成，2026-03-13）

架构重构：
- ✅ Viewer 组件化拆分（App.tsx ~270 行 + hooks + components）
- ✅ EngineContext 统一回调传递
- ✅ LLM 提供者抽象层（LLMProvider 接口）
- ✅ Replay 版本化（schema_version "1.0"）
- ✅ 项目 CLAUDE.md 与文档体系

功能补齐：
- ✅ Viewer 错误面板（ErrorPanel 按类型着色）
- ✅ Treaty joint_attack 执行逻辑（签约/攻击加成/互不侵犯）
- ✅ Replay 筛选（按模式、agent 数量，API + UI）
- ✅ 排名面板显示角色名
- ✅ CLI --help 与参数校验改进
- ✅ Mock Agent 消息个性化（确定性模板 + DNA 驱动）
- ✅ 核心模块单元测试（8 文件，21 用例）

详见 `docs/superpowers/specs/` 下的设计文档。

## V0.3 建议方向

- 支持分享链接、导出视频/GIF、精选局收藏
- 支持更丰富的世界规则与事件系统
- 支持多主题剧本模板和角色池模板

## 16. 待确认问题

### 已决策
- ~~后续优先级更高的是 Viewer 易用性，还是模拟规则深度？~~ → V0.2 以架构重构优先，功能按依赖排序推进。

### 待确认
1. 本项目长期更偏”创作工具 / Demo 引擎”还是”可持续游玩的观战产品”？
2. 是否需要为 profiles 建立更标准的编辑和验证工具链？
3. 是否计划将 replay API 独立成远程服务，而不只服务本地目录？
4. LLM 提供者抽象后，第二个要支持的模型是 OpenAI、Claude 还是其他？
5. Viewer 组件化后是否引入状态管理库（如 Zustand），还是继续用 props drilling？

## 17. 结论

AI Pixel War 已经具备鲜明的产品雏形，不只是一个技术实验仓库。它当前最强的价值，在于把 AI 多角色行为、关系叙事和像素艺术生成组织成一套“可运行、可观看、可复盘”的完整体验。

本 PRD 建议团队继续沿着以下主线推进：

1. 稳定模拟引擎与 replay 协议
2. 强化 Viewer 的讲故事能力与复盘能力
3. 降低配置门槛，让更多人能轻松创建自己的数字分身剧场
